###################################################

Install the few requirements in your terminal before running script

pip install -r requirements.txt
pip install --upgrade google-api-python-client google-auth-httplib2 google-auth-oauthlib
pip install textblob



Got to google api console:
create a new project and under library, Search;

Youtube API v3

Dwonload the library and setup the AouthID
Setup the API Key 
- make sure the executable device is set to Desktop app
- Setup the API key and download the json file.

- Make sure json file is in the same directory as the python script.
- RENAME THE JSON FILE: "desktop.json"

- Run the flask application locally.

- Once up, You can query from React.